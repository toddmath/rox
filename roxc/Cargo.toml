[package]
name = "roxc"
version = "0.1.0"
edition = "2021"
description = "Compiler, parser, lexer and interpreter library for the rox langauge"
license = "MIT OR Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
